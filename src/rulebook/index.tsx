// Self-contained rulebook. Phase 6 fills this in.
//
// Renders TSX topics with inline SVG diagrams. Topics are searchable.

export function Rulebook() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Rulebook</h2>
      <p style={{ color: 'var(--ink-muted)' }}>
        Phase 6 — full rulebook with diagrams. For now, see <code>CLAUDE.md</code> and{' '}
        <code>ghost stories rules.pdf</code>.
      </p>
    </div>
  )
}
